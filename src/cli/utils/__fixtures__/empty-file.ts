// This file intentionally contains no declarations
// It's used to test error cases when symbols are not found

const internalHelper = () => {
  console.log('This is just an internal helper, not a declaration');
};
